class LeapYear {
    public void main() {
		int year;
		year = 2000;

        if (year % 4 == 0 && year % 100 != 0 || year % 400 == 0) {
            print(year, "is leap year.");
        }
        else {
            print(year, "is not leap year.");
        }
    }
}

